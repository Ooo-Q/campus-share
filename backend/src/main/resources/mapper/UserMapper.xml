<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campusshare.mapper.UserMapper">

    <resultMap id="UserResult" type="com.campusshare.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="role" column="role"/>
        <result property="nickname" column="nickname"/>
        <result property="avatar" column="avatar"/>
        <result property="gender" column="gender"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="lastLoginAt" column="last_login_at"/>
    </resultMap>

    <select id="findById" resultMap="UserResult">
        SELECT * FROM sys_user WHERE id = #{id}
    </select>

    <select id="findByUsername" resultMap="UserResult">
        SELECT * FROM sys_user WHERE username = #{username}
    </select>

    <select id="search" resultMap="UserResult">
        SELECT * FROM sys_user
        <where>
            <if test="keyword != null and keyword != ''">
                (username LIKE CONCAT('%', #{keyword}, '%')
                OR nickname LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="role != null and role != ''">
                AND role = #{role}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

    <insert id="insert" parameterType="com.campusshare.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user(username, password, role, nickname, avatar, gender, phone, email, created_at, updated_at, last_login_at)
        VALUES (#{username}, #{password}, #{role}, #{nickname}, #{avatar}, #{gender}, #{phone}, #{email}, NOW(), NOW(), #{lastLoginAt})
    </insert>

    <update id="update" parameterType="com.campusshare.entity.User">
        UPDATE sys_user
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="role != null">role = #{role},</if>
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="avatar != null">avatar = #{avatar},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="email != null">email = #{email},</if>
            <if test="lastLoginAt != null">last_login_at = #{lastLoginAt},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM sys_user WHERE id = #{id}
    </delete>
</mapper>

